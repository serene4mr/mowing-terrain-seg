# Core DL stack
numpy<2
torch==2.1.2
torchvision==0.16.2

# Install mmcv wheel from OpenMMLab index
-f https://download.openmmlab.com/mmcv/dist/cu118/torch2.1/index.html
mmcv==2.1.0

# OpenMMLab libs
mmdet==3.3.0
mmsegmentation==1.2.2

# MMDeploy – version must match the exported model (detail.json → "version": "1.3.1")
#
# ONNX Runtime is NOT installed via pip here. The Dockerfile downloads the
# official C++ SDK tarball (onnxruntime-linux-x64-gpu-1.15.1.tgz) to
# /opt/onnxruntime and sets ONNXRUNTIME_DIR + LD_LIBRARY_PATH.
# This is the approach prescribed by the MMDeploy docs and avoids the
# glibc VERNEED/NEEDED mismatch present in the onnxruntime-gpu pip wheel.
mmdeploy==1.3.1
mmdeploy-runtime-gpu==1.3.1
protobuf<=3.20.2

# Extra deps
openmim
ftfy
regex