{
  "name": "mowing-terrain-seg-dev-env",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "args": {
      "USERNAME": "${localEnv:USER}",
      "BASE_IMAGE": "ghcr.io/serene/mowing-terrain-seg:base"
    }
  },

  "remoteUser": "${localEnv:USER}",
  "updateRemoteUserUID": true,

  "runArgs": [
    "--privileged",
    "--name", "mowing-terrain-seg-dev-env",
    "--net", "host",
    "--gpus", "all",
    "--shm-size=16g",
    "-e", "PYTHONUNBUFFERED=1",
    "-e", "DISPLAY=:0",
    "-v", "/tmp/.X11-unix:/tmp/.X11-unix:rw",
    "-v", "/dev:/dev"
  ],

  // Python deps are in the base image (docker/Dockerfile → mowing-seg:base). Uncomment only if you want
  // to reinstall after a requirements.txt change without rebuilding the image
  // "postCreateCommand": "pip install -r requirements.txt",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-python.autopep8"
      ]
    }
  }
}
  