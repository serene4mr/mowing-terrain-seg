# The active Dockerfile for this project lives at:
#   docker/Dockerfile
#
# devcontainer.json points there with context: ".." (workspace root).
# This file is kept as a placeholder only.
#
# To rebuild the dev container:
#   Dev Containers: Rebuild Container  (Ctrl+Shift+P)
